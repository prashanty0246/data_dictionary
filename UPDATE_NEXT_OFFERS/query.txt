const selectMobankNextOffers = `SELECT
Offer_ID1,
Offer_ID2,
Offer_ID3,
Offer_ID4,
Offer_ID5,
Offer_ID6,
Offer_ID7
FROM recommendation_new
WHERE customer_code = @customer_code`

const updateMobankNextOffers = (
  columnToUpdate
) => `UPDATE Recommendation_New  SET ${columnToUpdate} = 'Y'
WHERE customer_code = @customer_code`

module.exports = {
  updateMobankNextOffers,
  selectMobankNextOffers,
}
